import { Input } from "../shared/uikit/Input";
import { Field } from "../shared/uikit/Field";
import { Button } from "../shared/uikit/Button";

export default function () {
  return (
    <section class="gap-3xl m-auto flex min-h-full min-w-24 flex-col justify-center px-0">
      <h2 class="text text-left text-neutral-500">Сздание аккаунта</h2>
    </section>
  );
}
